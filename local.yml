- name: Setting Up PC
  hosts: localhost
  connection: local
  become: true
  become_method: sudo
  become_user: brayan

  pre_tasks:
    - import_tasks: ansible/tasks/pacman_update_arch.yml
      become: true
      become_user: root
